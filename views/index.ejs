<!DOCTYPE html>
<html>

<head>
    <title>Real-time Code Editor</title>
    <link href="/output.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>h1, h2, h3, h4, h5, h6 { font-family: 'Inter', sans-serif; --font-sans: 'Inter'; }
    .jacquard-24-regular {
            font-family: 'Jacquard 24', system-ui;
            font-weight: 400;
            font-style: normal;
        }
    </style>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jacquard+24&display=swap" rel="stylesheet">
</head>

<body class="bg-gray-200 pt-20">
    <header class="fixed top-0 left-0 right-0 bg-gradient-to-r from-purple-400 via-pink-500 to-red-500 shadow-md z-50 flex items-center justify-between px-4 py-2">
        <h1 class="text-2xl font-bold text-white">Real-time Code Editor</h1>
        <% if (name) { %>
            <p class="text-lg text-white font-semibold"><%= name %></p>
        <% } %>
        <div id="userList" class="text-white"></div>
        <a href="https://ekanshrajput.netlify.com/" class="text-white hover:text-gray-200 transition-colors duration-200">
            <i class="material-symbols-outlined text-4xl">account_circle</i>
        </a>
    </header>
    <main class="pt-10">
    <h1 class="text-center text-7xl font-bold mb-10 jacquard-24-regular">Real-time Code Editor</h1>
    <div class="flex">
        <div class="w-1/2 p-4 bg-white rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold mb-4">Input</h2>
            <select id="language" class="w-full mb-4 p-2 border border-gray-300 rounded">
                <option value="javascript">JavaScript</option>
                <option value="python">Python</option>
                <option value="java">Java</option>
                <option value="c">C</option>
                <option value="c++">C++</option>
                <option value="bash">Bash</option>
            </select>
            <input id="className" type="text" placeholder="Class name (for Java)" class="w-full mb-4 p-2 border border-gray-300 rounded">
            <textarea id="code" class="w-full h-64 mb-4 p-2 border border-gray-300 rounded"></textarea>
            <button id="compile" class="w-full py-2 bg-blue-500 text-white rounded hover:bg-blue-600">Compile</button>
        </div>
        <div class="w-1/2 p-4 bg-white rounded-lg shadow-md ml-4">
            <h2 class="text-2xl font-semibold mb-4">Output</h2>
            <pre id="output" class="w-full h-full bg-gray-100 p-2 rounded"></pre>
        </div>
    </div>
    </main>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/main.js"></script>
</body>

</html>